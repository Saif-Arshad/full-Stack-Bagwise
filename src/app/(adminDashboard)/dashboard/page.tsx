"use client"
import { useSelector } from "react-redux"

import React from 'react'

function page() {

  return (
    <div>
    {/* <div className={`${isOpen === false ? "lg:ml-[90px]" : "lg:ml-72"}`}> */}
      hey This is dashboard
    </div>
  )
}

export default page
